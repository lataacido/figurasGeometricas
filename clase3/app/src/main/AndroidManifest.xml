<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clase3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".quien"></activity>
        <activity android:name=".Escaleno" />
        <activity android:name=".Isosceles" />
        <activity android:name=".equilatero" />
        <activity android:name=".Rombo" />
        <activity android:name=".Triangulo" />
        <activity android:name=".Circulo" />
        <activity android:name=".Cuadrado" />
        <activity
            android:name=".menu"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Mostar" />
        <activity android:name=".Calculadora">

            <!-- paraejecutar primero -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>